v0.1.0 
node sort.js filename
node sort.js wrongFilename
node sort.js filename(emptyFile)

--done-- create sortLib and testSortLib.
--done-- create loadContent to read content from the file. 
--done-- create sortContent to sort given fileLines. 
--done-- modify loadContent to detect error if the file path is not present and return the error. 
--done-- remove the logic of deciding the error printing from main.
--done-- check the behavior for emptyFile.

------------------------------------------------------------------------------------------------------------------

v0.2.0 
node sort.js *** how to test this case???
node sort.js < cat filename 
cat filename | node sort.js
node sort.js < echo "a\n1\nB"

--done-- write appTestFor these above conditions first

--done-- add stdin to the sort.

  --done-- modify readFileSync to readFile and test.
  --done-- change the flow to be asynchronous.

  --done-- add a branching to read from stdin in loadContent.
  --done-- test with having to read from stdin.
  --done-- make loadFile responsible for displaying error. 


refactoring v0.2.0

  * modify sort to return.
  * create a new loadContent in utils to initialize the load the content and send it to sortContent. 
  * make loadContent to display the result in the end block.
  * modify the tests for sortLib.
  * wire everything up.
    
